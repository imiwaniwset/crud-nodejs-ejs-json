<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body>
    <div
      class="bg-gradient-to-r from-teal-400 to-sky-400 w-full min-h-screen flex justify-center items-center shadow-lg"
    >
      <div class="max-w-xs p-5 bg-white">
        <h1
          class="text-center mb-3 text-xl bg-gradient-to-t py-2 text-slate-50 font-semibold"
        >
          Form Update Employee


        </h1>
        
        <form
          action="/employee/<%= employees.id %>/edit"
          method="POST" enctype="multipart/form-data"
        >
           <div>
            <input
              class="py-1 w-full border-b-2 border-b-teal-400 text-slate-600"
              type="file"
              placeholder="Foto"
              name="image"
              required
              readonly
            />
          <img src="<%=employees.image%>" class="w-16 h-16 object-cover drop-shadow" alt="">
        </div> 

        <input type="hidden" name="old_image" value="<%=employees.image%>">
          <div class="mb-2">
            <input
              class="px-2 py-1 w-full border-b-2 border-b-teal-400"
              type="text"
              name="nama"
              placeholder="nama"
              value="<%= employees.nama %>"
            />
          </div>
          <div>
            <input
              class="px-2 py-1 w-full border-b-2 border-b-teal-400 text-slate-600"
              type="date"
              placeholder="Tahun Masuk"
              name="tahun_masuk"
              value="<%= employees.tahun_masuk %>"
            />
          </div>

          <select
            id="jabatan"
            name="jabatan"
            
            class="px-2 py-1 w-full border-b-2 border-b-teal-400 text-slate-600 mt-2"
          >
          
          <option value="Frontend" <%= employees.jabatan === 'Frontend' ? 'selected' : '' %>>Frontend Dev</option>
          <option value="Backend" <%= employees.jabatan === 'Backend' ? 'selected' : '' %>>Backend Dev</option>
          <option value="Product Manager" <%= employees.jabatan === 'Product Manager' ? 'selected' : '' %>>Product Manager</option>
          
          </select>
        

          <div class="mt-5 mx-auto flex justify-center">
            <input
              type="submit"
              class="bg-gradient-to-r text-slate-50 from-sky-500 px-6 py-2 to-teal-500 rounded shadow-md"
              value="Update Employee"
            />
          </div>
        </form>
       TG67
      </div>
    </div>
  </body>
</html>
